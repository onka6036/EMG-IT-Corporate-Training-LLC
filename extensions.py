from flask_login import LoginManager

# Database removed: provide a simple placeholder for compatibility.
# Any real database operations have been removed; data is served from in-memory
# structures in `data.py`.
login_manager = LoginManager()
